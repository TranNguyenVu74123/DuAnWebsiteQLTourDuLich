@* @{
    // Lấy đường dẫn hiện tại
    string currentPagePath = Context.Request.Path;

    // Đường dẫn tương đối đến trang đăng nhập và đăng ký
    string loginPagePath = "/Identity/Account/Login";
    string registerPagePath = "/Identity/Account/Register";

    // Kiểm tra xem trang hiện tại có phải là trang đăng nhập hoặc đăng ký không
    bool isLoginPage = currentPagePath.Contains(loginPagePath);
    bool isRegisterPage = currentPagePath.Contains(registerPagePath);

    ViewData["IsLoginPage"] = isLoginPage;
    ViewData["IsRegisterPage"] = isRegisterPage;
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css" rel="stylesheet" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet" />
    <script src="https://www.google.com/recaptcha/api.js" async defer></script>
</head>
<body class="d-flex flex-column h-100">
    <header class="navbar navbar-expand navbar-dark sticky-top flex-column flex-md-row bd-navbar" style="background-color: white;">
        <div class="container">
            @if (!(bool)ViewData["IsLoginPage"] && !(bool)ViewData["IsRegisterPage"])
            {
                <a class="navbar-brand mr-0 mr-md-2" href="/" aria-label="Bootstrap">
                    <img src="../../LayoutAdmin/assets/images/saigon-glisten.jpg" alt="logo" style="width: 70px; height: 70px;" />
                </a>
            }
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav bd-navbar-nav flex-row me-auto">
                    @if (!(bool)ViewData["IsLoginPage"] && !(bool)ViewData["IsRegisterPage"])
                    {
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index">Trang chủ</a>
                        </li>
                    }

                    @if (!(bool)ViewData["IsLoginPage"] && !(bool)ViewData["IsRegisterPage"])
                    {
                        <li class="nav-item">
                            <form id="searchForm" class="search-container" asp-controller="Home" asp-action="Search" method="post" autocomplete="on">
                                <div class="input-group">
                                    <input id="searchInput" class="form-control searchInput me-2" type="search" placeholder="Tìm kiếm" aria-label="Search" name="query">
                                    <button id="microphoneButton" type="button" class="btn btn-outline-secondary">
                                        <i class="fas fa-microphone"></i>
                                    </button>
                                    <ul id="autocompleteDropdown" class="autocompleteDropdown" aria-labelledby="searchInput"></ul>
                                    <button id="searchButton" class="btn btn-primary" type="submit">Tìm kiếm</button>
                                </div>
                            </form>
                        </li>
                    }

                    <!-- Thêm phần dịch ngôn ngữ -->
                    <div id="google_translate_element"></div>

                    <script type="text/javascript">
                        function googleTranslateElementInit() {
                            new google.translate.TranslateElement({
                                pageLanguage: 'vi',
                                includedLanguages: 'af,sq,am,ar,hy,az,eu,be,bn,bs,bg,ca,ceb,ny,zh-CN,zh-TW,co,hr,cs,da,nl,en,eo,es,et,fi,fr,fy,gl,ka,de,el,gu,ht,ha,he,hi,hmn,hu,is,ig,id,ga,it,ja,jw,kn,kk,km,ko,ku,ky,lo,la,lv,lt,lb,mk,mg,ms,ml,mt,mr,mn,my,ne,no,oc,ps,fa,pl,pt,pa,pri,ro,ru,sm,sa,sh,sn,sd,si,sk,sl,so,es,su,sw,sv,tg,ta,te,th,ti,tr,uk,ur,uz,vi,cy,xh,yi,yo,zu',
                                layout: google.translate.TranslateElement.InlineLayout.SIMPLE
                            }, 'google_translate_element');
                        }
                    </script>
                    <script type="text/javascript" src="//translate.google.com/translate_a/element.js?cb=googleTranslateElementInit"></script>

                    @if ((User.IsInRole("Admin") || User.IsInRole("Company")))
                    {
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="Admin" asp-controller="Product" asp-action="">Giao diện Quản Lý</a>
                        </li>
                    }
                </ul>
                <partial name="_LoginPartial" />

                @if (!(bool)ViewData["IsLoginPage"] && !(bool)ViewData["IsRegisterPage"] && User.IsInRole("Customer"))
                {
                    <form asp-action="Index" asp-controller="ShoppingCart" class="d-flex">
                        <button class="btn btn-outline-dark" type="submit">
                            <i class="fas fa-suitcase me-1"></i>
                            Tour của bạn
                        </button>
                    </form>
                }
            </div>
        </div>
    </header>

    <main class="flex-shrink-0">
        <div class="container container-custom">
            @RenderBody()
        </div>
    </main>

    <footer class="footer mt-auto py-3 bg-body-tertiary">
        <div class="container">
            &copy; 2024 - SAIGON GLISTEN - Nơi đưa bạn đến với những chuyến du lịch thú vị
        </div>
    </footer>

    <!-- Owl Carousel JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/owl.carousel.min.js"></script>

    <!-- FancyBox JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/fancybox/3.5.7/jquery.fancybox.min.js"></script>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>

    <script src="~/LayoutHome/js/site.js" asp-append-version="true"></script> <!-- Đường dẫn tới file JavaScript từ template -->
    @await RenderSectionAsync("Scripts", required: false)
</body>

<!-- Custom CSS để cải thiện giao diện đa ngôn ngữ và ẩn chữ "Hỗ trợ bởi Google Dịch" -->
<style>
    #google_translate_element {
        display: flex;
        justify-content: center;
        align-items: center;
        margin-top: 10px;
    }

    .goog-te-gadget {
        color: transparent; /* Ẩn chữ "Hỗ trợ bởi Google Dịch" */
    }

    .goog-te-gadget .goog-te-combo {
        color: black; /* Hiển thị phần chọn ngôn ngữ rõ ràng */
        background-color: #f8f9fa;
        border: 1px solid #ced4da;
        border-radius: 5px;
        padding: 5px;
    }

    /* Tùy chỉnh kiểu dropdown */
    .goog-te-combo {
        font-size: 16px;
        font-family: 'Arial', sans-serif;
    }

    /* Ẩn logo của Google Dịch */
    .goog-logo-link {
        display: none !important;
    }

    .goog-te-gadget span {
        display: none !important;
    }

    /* Căn giữa dropdown chọn ngôn ngữ */
    .goog-te-combo {
        margin: 0 auto;
    }
</style>

<script>
    document.addEventListener("DOMContentLoaded", (event) => {
        const microphoneButton = document.getElementById('microphoneButton');
        const searchInput = document.getElementById('searchInput');

        let recognition = new webkitSpeechRecognition(); // For WebKit browsers like Chrome
        recognition.continuous = true;
        recognition.lang = 'vi-VN'; 

        let isRecognizing = false;

        recognition.onstart = () => {
            microphoneButton.classList.add('active');
        };

        recognition.onresult = (event) => {
            const transcript = event.results[event.results.length - 1][0].transcript;
            searchInput.value = transcript;
        };

        recognition.onerror = (event) => {
            console.error('Error occurred: ' + event.error);
            stopRecognition();
        };

        recognition.onend = () => {
            microphoneButton.classList.remove('active');
        };

        microphoneButton.addEventListener('click', () => {
            if (isRecognizing) {
                stopRecognition();
            } else {
                startRecognition();
            }
        });

        function startRecognition() {
            recognition.start();
            isRecognizing = true;
        }

        function stopRecognition() {
            recognition.stop();
            isRecognizing = false;
        }
    });
</script>

</html>
 *@

@{
    string currentPagePath = Context.Request.Path;
    string loginPagePath = "/Identity/Account/Login";
    string registerPagePath = "/Identity/Account/Register";
    bool isLoginPage = currentPagePath.Contains(loginPagePath);
    bool isRegisterPage = currentPagePath.Contains(registerPagePath);

    ViewData["IsLoginPage"] = isLoginPage;
    ViewData["IsRegisterPage"] = isRegisterPage;
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css" rel="stylesheet" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet" />
    <script src="https://www.google.com/recaptcha/api.js" async defer></script>
</head>
<body class="d-flex flex-column h-100 light-mode">
    <header class="navbar navbar-expand navbar-dark sticky-top flex-column flex-md-row bd-navbar" style="background-color: white;">
        <div class="container">
            @if (!(bool)ViewData["IsLoginPage"] && !(bool)ViewData["IsRegisterPage"])
            {
                <a class="navbar-brand mr-0 mr-md-2" href="/" aria-label="Bootstrap">
                    <img src="../../LayoutAdmin/assets/images/saigon-glisten.jpg" alt="logo" style="width: 70px; height: 70px;" />
                </a>
            }
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav bd-navbar-nav flex-row me-auto">
                    @if (!(bool)ViewData["IsLoginPage"] && !(bool)ViewData["IsRegisterPage"])
                    {
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index">Trang chủ</a>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="weatherForecast">Dự báo thời tiết</a>
                        </li>
                    }

                    @if (!(bool)ViewData["IsLoginPage"] && !(bool)ViewData["IsRegisterPage"])
                    {
                        <li class="nav-item">
                            <form id="searchForm" class="search-container" asp-controller="Home" asp-action="Search" method="post" autocomplete="on">
                                <div class="input-group">
                                    <input id="searchInput" class="form-control searchInput me-2" type="search" placeholder="Tìm kiếm" aria-label="Search" name="query">
                                    <button id="microphoneButton" type="button" class="btn btn-outline-secondary">
                                        <i class="fas fa-microphone"></i>
                                    </button>
                                    <ul id="autocompleteDropdown" class="autocompleteDropdown" aria-labelledby="searchInput"></ul>
                                    <button id="searchButton" class="btn btn-primary" type="submit">Tìm kiếm</button>
                                </div>
                            </form>
                        </li>
                    }

                    <!-- Phần dịch ngôn ngữ -->
                    <li class="nav-item mx-2">
                        <div id="google_translate_element"></div>

                        <script type="text/javascript">
                            function googleTranslateElementInit() {
                                new google.translate.TranslateElement({
                                    pageLanguage: 'vi',
                                    includedLanguages: 'en,fr,de,es,zh-CN,ja,ko,vi',
                                    layout: google.translate.TranslateElement.InlineLayout.SIMPLE
                                }, 'google_translate_element');
                            }
                        </script>
                        <script type="text/javascript" src="//translate.google.com/translate_a/element.js?cb=googleTranslateElementInit"></script>
                    </li>

                    <!-- Nút chuyển đổi chế độ -->
                    <li class="nav-item mx-2">
                        <button id="toggleMode" class="btn btn-outline-dark"><i class="fas fa-moon"></i></button>
                    </li>

                    @if (User.IsInRole("Admin") || User.IsInRole("Company"))
                    {
                        <li class="nav-item mx-2">
                            <a class="nav-link text-dark" asp-area="Admin" asp-controller="Product" asp-action="">Giao diện Quản Lý</a>
                        </li>
                    }
                </ul>

                <partial name="_LoginPartial" />

                @if (!(bool)ViewData["IsLoginPage"] && !(bool)ViewData["IsRegisterPage"] && User.IsInRole("Customer"))
                {
                    <form asp-action="Index" asp-controller="ShoppingCart" class="d-flex">
                        <button class="btn btn-outline-dark" type="submit">
                            <i class="fas fa-suitcase me-1"></i>
                            Tour của bạn
                        </button>
                    </form>
                }
            </div>
        </div>
    </header>

    <main class="flex-shrink-0">
        <div class="container container-custom">
            @RenderBody()
        </div>
    </main>

    <footer class="footer mt-auto py-3 bg-body-tertiary">
        <div class="container">
            &copy; 2024 - SAIGON GLISTEN - Nơi đưa bạn đến với những chuyến du lịch thú vị
        </div>
    </footer>

    <!-- Owl Carousel JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/owl.carousel.min.js"></script>

    <!-- FancyBox JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/fancybox/3.5.7/jquery.fancybox.min.js"></script>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>

    <script src="~/LayoutHome/js/site.js" asp-append-version="true"></script>

    @await RenderSectionAsync("Scripts", required: false)

    @RenderSection("Styles", required: false)



    <script>
        $(document).ready(function () {
            // Mặc định chế độ sáng
            $('body').addClass('light-mode');

            // Nút chuyển đổi chế độ
            $('#toggleMode').click(function () {
                $('body').toggleClass('dark-mode light-mode');

                // Cập nhật biểu tượng khi chuyển đổi
                if ($('body').hasClass('dark-mode')) {
                    $('#toggleMode').html('<i class="fas fa-sun"></i>');
                } else {
                    $('#toggleMode').html('<i class="fas fa-moon"></i>');
                }
            });
        });
    </script>

    <script>
    document.addEventListener("DOMContentLoaded", (event) => {
        const microphoneButton = document.getElementById('microphoneButton');
        const searchInput = document.getElementById('searchInput');

        const recognition = new (window.SpeechRecognition || window.webkitSpeechRecognition)(); // Sử dụng SpeechRecognition cho cả Chrome và các trình duyệt khác
        recognition.continuous = false; // Dừng nhận diện khi xong
        recognition.lang = 'vi-VN';

        let isRecognizing = false;

        recognition.onstart = () => {
            microphoneButton.classList.add('active');
        };

        recognition.onresult = (event) => {
            const transcript = event.results[0][0].transcript; // Lấy kết quả đầu tiên
            searchInput.value = transcript; // Điền vào ô tìm kiếm
            stopRecognition(); // Dừng nhận diện sau khi nhận xong
        };

        recognition.onerror = (event) => {
            console.error('Error occurred: ' + event.error);
            stopRecognition();
        };

        recognition.onend = () => {
            microphoneButton.classList.remove('active');
            isRecognizing = false; // Đặt trạng thái nhận diện là false
        };

        microphoneButton.addEventListener('click', () => {
            if (isRecognizing) {
                stopRecognition();
            } else {
                startRecognition();
            }
        });

        function startRecognition() {
            recognition.start();
            isRecognizing = true;
        }

        function stopRecognition() {
            recognition.stop();
        }
    });
</script>



    <style>
        /* Nền sáng */
        body.light-mode {
            background-color: white;
            color: black;
        }

        /* Nền tối */
        body.dark-mode {
            background-color: #121212;
            color: navy;
        }

        /* Căn chỉnh các nút Google Dịch, Dark/Light Mode và Tìm kiếm cách đều nhau */
        .navbar-nav .nav-item {
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .toggle-mode {
            padding: 15px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 50px;
            cursor: pointer;
        }
    </style>
</body>
</html>
